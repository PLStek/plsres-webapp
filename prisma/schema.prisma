generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

enum CharbonStatus {
    SCHEDULED
    ONGOING
    FINISHED
}

model Charbon {
    id             Int           @id @default(autoincrement())
    status         CharbonStatus @default(SCHEDULED)
    isDraft        Boolean       @default(true)
    name           String
    description    String
    timestamp      DateTime
    replayUrl      String?       @unique
    courseId       Int
    discordEventId String        @unique

    resources   Resource[]
    actionneurs CharbonActionneur[]
    course      Course              @relation(fields: [courseId], references: [id], onDelete: Restrict)
    attendees   CharbonAttendee[]

    @@index([courseId], name: "courseId")
}

model CharbonAttendee {
    id        Int    @id @default(autoincrement())
    charbonId Int
    discordUserId String

    charbon Charbon @relation(fields: [charbonId], references: [id], onDelete: Cascade)

    @@index([charbonId], name: "charbonId")
    @@unique([charbonId, discordUserId])
}

model Course {
    id                       Int    @id @default(autoincrement())
    code                     String @unique
    name                     String
    discordResourceChannelId String @unique
    discordVoiceChannelId    String @unique

    category CourseCategory
    charbon  Charbon[]
}

enum CourseCategory {
    MATH
    ELEC
    INFO
    MECA
}

model Resource {
    id        Int    @id @default(autoincrement())
    name      String
    charbonId Int
    extension String

    charbon Charbon @relation(fields: [charbonId], references: [id], onDelete: Cascade)
}

model Actionneur {
    id         Int     @id @default(autoincrement())
    discordId  String  @unique
    username   String  @unique
    isAdmin    Boolean @default(false)
    isActive   Boolean @default(true)
    secretHash String

    charbons CharbonActionneur[]
}

model Invitation {
    id        Int      @id @default(autoincrement())
    token     String   @unique
    discordId String   @unique
    expiresAt DateTime
}

model CharbonActionneur {
    charbon   Charbon @relation(fields: [charbonId], references: [id], onDelete: Cascade)
    charbonId Int

    actionneur   Actionneur @relation(fields: [actionneurId], references: [id], onDelete: Cascade)
    actionneurId Int

    @@id([charbonId, actionneurId])
}

model RevokedToken {
    id        Int      @id @default(autoincrement())
    token     String   @unique
    expiresAt DateTime

    @@index([expiresAt])
}
